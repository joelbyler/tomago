# ~/.tomago/<%= project_name %>.yml

name: <%= project_name %>
root: ~/

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - git status
  - server: bundle exec rails s
  - logs: tail -f log/development.log
